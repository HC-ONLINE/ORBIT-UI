---
import BaseLayout from "@layouts/BaseLayout.astro";
import NavLink from "@components/ui/NavLink.astro";

const baseURL = import.meta.env.BASE_URL;
---

<BaseLayout
  title="Components · orbit-ui"
  description="Listado de componentes disponibles en orbit-ui"
>
  <section class="mx-auto max-w-4xl px-6 py-12 space-y-12">
    <!-- Header -->
    <header class="space-y-2">
      <h1 class="text-2xl font-semibold">Components</h1>
      <p class="text-sm text-(--color-text-muted)">
        Componentes base disponibles en orbit-ui. Cada uno tiene un contrato
        explícito y documentación dedicada.
      </p>
    </header>

    <!-- Listado -->
    <section class="space-y-6">
      <div class="space-y-8">
        <NavLink href={`${baseURL}/components/card`}> Card </NavLink>
        <p class="text-sm text-(--color-text-muted)">
          Contenedor estructural para agrupar contenido. No interactivo por
          defecto.
        </p>
      </div>

      <div class="space-y-8">
        <NavLink href={`${baseURL}/components/button`}> Button </NavLink>
        <p class="text-sm text-(--color-text-muted)">
          Componente de acción. Ejecuta eventos. No navega.
        </p>
      </div>

      <div class="space-y-8">
        <NavLink href={`${baseURL}/components/navLink`}> NavLink </NavLink>
        <p class="text-sm text-(--color-text-muted)">
          Enlace de navegación con estado activo persistente.
        </p>
      </div>

      <div class="space-y-8">
        <NavLink href={`${baseURL}/components/alert`}> Alert </NavLink>
        <p class="text-sm text-(--color-text-muted)">
          Mensaje informativo para comunicar estado o contexto.
        </p>
      </div>

      <div class="space-y-8">
        <NavLink href={`${baseURL}/components/badge`}> Badge </NavLink>
        <p class="text-sm text-(--color-text-muted)">
          Etiqueta compacta para estados o categorías. Uso inline.
        </p>
      </div>

      <div class="space-y-8">
        <NavLink href={`${baseURL}/components/input`}> Input </NavLink>
        <p class="text-sm text-(--color-text-muted)">
          Campo de entrada de texto básico. Permite al usuario ingresar datos.
        </p>
      </div>
    </section>
  </section>
</BaseLayout>
