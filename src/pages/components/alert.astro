---
import BaseLayout from "@layouts/BaseLayout.astro";
import AlertComponent from "@components/ui/Alert.astro";
import ButtonComponent from "@components/ui/Button.astro";
---

<BaseLayout
  title="Alert · orbit-ui"
  description="Contrato y uso correcto del componente Alert"
>
  <section class="mx-auto max-w-4xl px-6 py-12 space-y-12">
    <header class="space-y-2">
      <h1 class="text-2xl font-semibold">Alert</h1>
      <p class="text-sm text-(--color-text-muted)">
        Componente informativo para comunicar estado o contexto.
      </p>
    </header>

    <section class="space-y-4">
      <h2 class="text-lg font-medium">Variantes</h2>

      <div class="space-y-4">
        <AlertComponent> Mensaje informativo por defecto. </AlertComponent>

        <AlertComponent variant="success">
          Operación completada correctamente.
        </AlertComponent>

        <AlertComponent variant="warning">
          Atención: revisa los datos ingresados.
        </AlertComponent>

        <AlertComponent variant="error">
          Error crítico. La acción no pudo completarse.
        </AlertComponent>
      </div>
    </section>

    <section class="space-y-4">
      <h2 class="text-lg font-medium">Con título</h2>

      <AlertComponent variant="warning">
        <span slot="title">Advertencia</span>
        Este cambio es irreversible.
      </AlertComponent>
    </section>

    <section class="space-y-4">
      <h2 class="text-lg font-medium">Con acciones</h2>

      <p class="text-sm text-(--color-text-muted)">
        Alert puede incluir acciones opcionales mediante un slot dedicado. El
        componente no define ni controla las acciones; solo las aloja.
      </p>
      <ul class="text-sm text-(--color-text-muted) list-disc pl-5 space-y-1">
        <li>Las acciones son <strong>opcionales</strong>.</li>
        <li>Alert no conoce ni importa componentes de acción.</li>
        <li>Las acciones se renderizan solo si el slot existe.</li>
      </ul>
      <AlertComponent variant="warning">
        <span slot="title">Advertencia</span>
        Esta acción no se puede deshacer.

        <div slot="actions">
          <ButtonComponent variant="secondary">Cancelar</ButtonComponent>
          <ButtonComponent>Continuar</ButtonComponent>
        </div>
      </AlertComponent>
    </section>

    <section class="space-y-4">
      <h2 class="text-lg font-medium">Antipatrón</h2>

      <p class="text-sm text-(--color-text-muted)">
        No uses Alert como toast, modal ni como contenedor de flujos complejos.
      </p>
    </section>
  </section>
</BaseLayout>
